import {StyleSheet} from 'react-native';
import {ss} from '../../theme/scaledPixel';
import {theme} from '../../theme/theme';

export const styles = StyleSheet.create({
  featuresList: {
    paddingHorizontal: ss(24),
  },
  featuresListSeparator: {
    width: ss(16),
  },
});
